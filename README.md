# examples

Different ways of interacting with the Wavedash SDK.

## Included examples

- `example-zig`: Zig + WebAssembly Pong demo with staged Wavedash loading, SDK init, and a local fallback shim for standalone browser testing.
